# ─── Required (core) ───────────────────────────────────────────────────────
DATABASE_URL="postgresql://..."
NEXT_PUBLIC_SUPABASE_URL="https://xxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."
# Optional: for delete-account to remove user from Supabase Auth
# SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# ─── Stripe (required for purchases, subscriptions, billing portal) ─────────
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Pro subscription (optional; omit if not using Pro)
STRIPE_PRO_PRICE_ID="price_..."

# Webhook (optional for local dev; use Stripe CLI to forward events)
STRIPE_WEBHOOK_SECRET="whsec_..."

# ─── Optional (defaults shown) ──────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
PLATFORM_FEE_PERCENT=5
PRO_FEE_PERCENT=2
BOOST_PRICE_CENTS=299
FREE_LISTING_LIMIT=5
